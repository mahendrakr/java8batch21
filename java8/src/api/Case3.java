package api;
/**
 * Inbounded type you can not extend more than 1 class at a time.
 *  Case3<T extends A , B> this is valid ? 
 *  because in general you can define n no. of parameter type here (T extends A) is one paramter type
 *  and (B) is another parameter type. 
 * @author Yashwani
 *
 * @param <T>
 */


//public class Case3<T extends A & B> {
	

//}
